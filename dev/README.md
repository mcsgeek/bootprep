# Development Files

Everything under `dev/` is used only for development and regression verification.

These files are not required to install, run, or operate BootPrep.

Run the local regression tests from the repository root with:

```bash
python3 -m unittest discover -s dev/tests -v
```
